# Copy this file to .env and fill in real values. .env is git-ignored —
# never commit real credentials.

# GitHub personal access token. Needs read access to the target repo always;
# needs write/release-creation access too only if you use --publish.
GITHUB_TOKEN=

# Anthropic API key, used to generate the release notes/PR summaries.
ANTHROPIC_API_KEY=
